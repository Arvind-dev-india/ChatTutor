// codeblock prefab - TODO: Implement rendering logic
// See docs/codeblock.doc.ts for DSL specification

import { definePrefab } from '@dsl/renderer-core'

export const codeblockPrefab = definePrefab('codeblock', (props, ctx) => {
  // Placeholder implementation
  // Will be implemented with canvas rendering in subsequent phases
  console.log('codeblock prefab called with props:', props)
})
