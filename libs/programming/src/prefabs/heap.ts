// heap prefab - TODO: Implement rendering logic
// See docs/heap.doc.ts for DSL specification

import { definePrefab } from '@dsl/renderer-core'

export const heapPrefab = definePrefab('heap', (props, ctx) => {
  // Placeholder implementation
  // Will be implemented with canvas rendering in subsequent phases
  console.log('heap prefab called with props:', props)
})
