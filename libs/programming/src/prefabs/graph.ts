// graph prefab - TODO: Implement rendering logic
// See docs/graph.doc.ts for DSL specification

import { definePrefab } from '@dsl/renderer-core'

export const graphPrefab = definePrefab('graph', (props, ctx) => {
  // Placeholder implementation
  // Will be implemented with canvas rendering in subsequent phases
  console.log('graph prefab called with props:', props)
})
